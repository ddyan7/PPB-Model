# Optional conda environment (alternative to the venv + requirements.txt route).
# The reference environment used for development is a Python 3.12 venv; see README.md.
name: ppb-model
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pandas>=2.0
  - numpy>=1.26
  - scikit-learn>=1.4
  - rdkit>=2023.9
  - matplotlib>=3.8
  - scipy>=1.11
  - joblib>=1.3
  - xgboost>=2.0
  - optuna>=3.5
  - pyyaml>=6.0
  - pip
